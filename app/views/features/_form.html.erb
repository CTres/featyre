<%= simple_form_for(@feature, :html => { :class => "form-vertical"}) do |f| %>
	<%= f.input :title%>
	<%= f.input :description %>
	<%= f.input :url %>
	<%= f.input :tag_list %>
	<%= f.button :submit %>
	<% end %>
