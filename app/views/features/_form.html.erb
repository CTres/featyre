<%= simple_form_for(@feature, :html => { :class => "form-vertical"}) do |f| %>
	<%= f.input :title%>
	<%= f.input :description %>
	<%= f.input :url %>
	<%= f.input :tag_list %>
	<%= f.input :text %>

	<%= f.button :submit %>
	<% end %>

	<script type="text/javascript">
	$('#text').wysihtml5();
</script>