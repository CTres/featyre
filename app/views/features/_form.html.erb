<%= simple_form_for(@feature, :html => {:class => 'form-horizontal' }) do |f| %>
	<%= f.input :title%>
	<%= f.input :description %>
	<%= f.input :url %>
	<%= f.input :tag_list %>
	<%= f.association :collaborators, label_method: :username %>
	<%= f.input :text %>
	<%= f.button :submit %>
<% end %>

